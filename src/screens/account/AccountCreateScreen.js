import React from "react";
import { Text, StyleSheet, View } from "react-native";

const AccountCreateScreen = () => {
  return (
    <View>
      <Text>AccountCreateScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default AccountCreateScreen;
